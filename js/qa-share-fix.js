(() => {
  const $=s=>document.querySelector(s);
  const textOf=e=>(e?.innerText||e?.textContent||'').replace(/\s+/g,' ').trim();
  const card=()=>$('#resultCard');
  const resultText=()=>{const c=card();return c&&textOf(c)?`Pakistan Livestock Weight Calculation Result\n${textOf(c)}`:''};
  const toast=msg=>{let n=$('#qaToast');if(!n){n=document.createElement('div');n.id='qaToast';n.setAttribute('role','status');document.body.append(n)}n.textContent=msg;n.classList.add('show');clearTimeout(n._t);n._t=setTimeout(()=>n.classList.remove('show'),2600)};
  const download=(blob,name)=>{if(!blob)return toast('Export could not be created.');const u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=name;a.rel='noopener';a.style.display='none';document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),2000)};
  const find=(kind)=>{const map={share:/^(share result|shared|شیئر کریں|شیئر رزلٹ)$/i,whatsapp:/whatsapp|واٹس ?ایپ/i,image:/export image|تصویر محفوظ/i,pdf:/export pdf|^pdf$/i,csv:/export csv|^csv$/i};return [...document.querySelectorAll('button')].find(b=>map[kind]?.test(textOf(b)))||null};
  const ensureIds=()=>{[['share','shareBtn'],['whatsapp','whatsappBtn'],['image','imageBtn'],['pdf','pdfBtn'],['csv','csvBtn']].forEach(([k,id])=>{const b=$('#'+id)||find(k);if(b&&!b.id)b.id=id})};
  const whatsapp=()=>{const t=resultText();if(!t)return toast('Calculate a result first.');const u='https://wa.me/?text='+encodeURIComponent(t);const w=window.open(u,'_blank','noopener,noreferrer');if(!w)location.href=u};
  const share=async()=>{const t=resultText();if(!t)return toast('Calculate a result first.');if(navigator.share){try{await navigator.share({title:'Pakistan Livestock Weight Result',text:t});toast('Result shared.');return}catch(e){if(e?.name==='AbortError')return}}try{await navigator.clipboard.writeText(t);toast('Result copied. Paste it into WhatsApp or any app.')}catch{whatsapp()}};
  const csv=()=>{const c=card();if(!c)return toast('Calculate a result first.');const rows=[...c.querySelectorAll('tr')].map(tr=>[...tr.children].map(td=>'"'+textOf(td).replace(/"/g,'""')+'"').join(','));download(new Blob(['"Pakistan Livestock Weight Calculation Result"\n',...rows].join('\n'),{type:'text/csv;charset=utf-8'}),'livestock-weight-result.csv');toast('CSV exported.')};
  const pdf=()=>{if(!card())return toast('Calculate a result first.');window.print()};
  const image=()=>{const c=card();if(!c)return toast('Calculate a result first.');const lines=(textOf(c).match(/.{1,62}(?:\s|$)/g)||['Pakistan Livestock Weight Calculation Result']);const esc=s=>s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');const W=1080,H=Math.max(720,190+lines.length*46);const body=lines.map((l,i)=>`<text x="72" y="${175+i*46}" font-family="Arial,sans-serif" font-size="${i===0?34:25}" font-weight="${i===0?700:400}" fill="#12352a">${esc(l)}</text>`).join('');const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${W}" height="${H}"><rect width="100%" height="100%" rx="32" fill="#f7f4ec"/><rect x="40" y="40" width="1000" height="100" rx="24" fill="#0b5a43"/><text x="72" y="105" font-family="Arial,sans-serif" font-size="32" font-weight="700" fill="#fff">Pakistan Livestock Weight</text>${body}</svg>`;const u=URL.createObjectURL(new Blob([svg],{type:'image/svg+xml'})),im=new Image();im.onload=()=>{const cv=document.createElement('canvas');cv.width=W*2;cv.height=H*2;const x=cv.getContext('2d');x.scale(2,2);x.drawImage(im,0,0);cv.toBlob(b=>{download(b,'livestock-weight-result.png');URL.revokeObjectURL(u);toast('Image exported.')},'image/png')};im.onerror=()=>{URL.revokeObjectURL(u);download(new Blob([svg],{type:'image/svg+xml'}),'livestock-weight-result.svg');toast('PNG unavailable; SVG exported instead.')};im.src=u};
  const bind=()=>{ensureIds();const map={share,whatsapp,image,pdf,csv};Object.entries(map).forEach(([k,fn])=>{const b=$('#'+k+'Btn');if(!b||b.dataset.qaBound)return;b.dataset.qaBound='1';b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();Promise.resolve(fn()).catch(()=>toast('Action failed. Please try again.'))},true)})};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();new MutationObserver(bind).observe(document.body,{subtree:true,childList:true});
})();
